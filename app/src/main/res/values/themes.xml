<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyCheckUp" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->DarkActionBar
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/main_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:navigationBarColor">@color/main_color</item>
    </style>

    <style name="MyButtonsColor" parent="@style/ShapeAppearanceOverlay.Material3.Button">
        <item name="colorPrimary">@color/btn_color</item>
    </style>
</resources>